"""Настройки. Всё, что может отличаться между ноутбуком, DL5 и площадкой, живёт здесь."""

from __future__ import annotations

import os
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent

WORK_DIR = Path(os.getenv("PRAXIS_WORK_DIR", ROOT / "work"))
# Собранный фронт. В докере пакет ставится в site-packages, поэтому путь задаётся явно.
WEB_DIST = Path(os.getenv("PRAXIS_WEB_DIST", ROOT / "web" / "dist"))
DB_PATH = WORK_DIR / "praxis.db"
VOCAB_PATH = os.getenv("PRAXIS_VOCAB") or None

# Требования кейса к входному видео.
# Верхняя граница длины ролика. Кейс обещает 10–30 с, но скрытый набор мы не видели,
# а отказ на 35-секундном ролике на финале хуже медленного прогона: 120 с — это тот же
# предел, что у времени обработки в кейсе.
MAX_DURATION_SEC = float(os.getenv("PRAXIS_MAX_DURATION", "120"))
MIN_HEIGHT = int(os.getenv("PRAXIS_MIN_HEIGHT", "720"))
ALLOWED_SUFFIXES = {".mp4", ".mov"}

# Какой сегментатор поднимать: stub пока пайплайна нет, дальше — реальный.
# Ядровой change-point со штрафом за сегмент. Выбран измерением: на 90 роликах даёт
# F1@0.5 0.507 против 0.429 у прежнего motion-dp, а число шагов выводит сам, тогда как
# базовым методам его приходится подсказывать. Полный разбор — experiments/research/BOUNDARIES.md.
PIPELINE = os.getenv("PRAXIS_PIPELINE", "learned-boundaries")

# Куда ходить за VLM. Пусто — значит семантика недоступна и работает запасной путь.
VLM_BASE_URL = os.getenv("PRAXIS_VLM_BASE_URL", "")
VLM_MODEL = os.getenv("PRAXIS_VLM_MODEL", "Qwen/Qwen3-VL-8B-Instruct")
CLIP_BASE_URL = os.getenv("PRAXIS_CLIP_BASE_URL", "")
# auto — генеративная модель, если она поднята, иначе классификатор, иначе без семантики.
# Порядок именно такой по измерению: на таксономии Assembly101 классификатор на SigLIP2
# показал 0.06 по глаголу против 0.40 у генеративной модели. На другой таксономии стоит
# перемерить: PRAXIS_NAMER=siglip переключает вручную.
NAMER = os.getenv("PRAXIS_NAMER", "auto")
CLIP_MODE = os.getenv("PRAXIS_CLIP_MODE", "factored")
CLIP_VERB_WEIGHT = float(os.getenv("PRAXIS_CLIP_VERB_WEIGHT", "1.0"))
CLIP_NOUN_WEIGHT = float(os.getenv("PRAXIS_CLIP_NOUN_WEIGHT", "1.0"))
# Одна фраза, описывающая снимаемый процесс. Модель без неё не знает, что за домен.
DOMAIN = os.getenv("PRAXIS_DOMAIN", "")
# Второй проход: оценка правдоподобия короткого списка гипотез вместо свободной генерации.
# Литература обещает от такой замены десятки пунктов, у нас измерение показало обратное:
# пара 0.209 без вычитания языкового приора и 0.194 с ним против 0.224 у свободной
# генерации, при удвоенном времени. Выключено, но оставлено: на другой модели и другом
# словаре стоит перемерить — приём сам по себе верный.
VLM_RESCORE = os.getenv("PRAXIS_VLM_RESCORE", "0").lower() not in {"0", "false", "no"}
# Трекер предмета (SAM2). Пусто — кадры режутся по общей рабочей зоне.
# Замысел был в том, чтобы показывать модели предмет крупно. Измерение на кухонном наборе:
# 0.209 / 0.179 / 0.090 против 0.358 / 0.463 / 0.224 без трекера. Кроп по предмету убирает
# контекст, без которого предмет не опознать, а рамка часто захватывает не то. Выключено.
TRACK_BASE_URL = os.getenv("PRAXIS_TRACK_BASE_URL", "")
TRACK_GRID = int(os.getenv("PRAXIS_TRACK_GRID", "3"))
TRACK_MIN_SHIFT = float(os.getenv("PRAXIS_TRACK_MIN_SHIFT", "0.05"))
TRACK_MARGIN = float(os.getenv("PRAXIS_TRACK_MARGIN", "0.35"))
# Второй проход, где модель видит свои же ответы про соседние шаги. Выключен: замер на
# кухонном наборе дал 0.313/0.448/0.209 против 0.358/0.463/0.224 без него, и вдвое дороже
# (40.6 с против 15 с на ролик). Модель начинает подгонять шаг под правдоподобную
# последовательность вместо того, что видит в кадре, — «закрыл» после «открыл» она
# дописывает по логике рассказа.
VLM_CONTEXT = os.getenv("PRAXIS_VLM_CONTEXT", "0").lower() not in {"0", "false", "no"}
# Подписи кадров позицией во времени: замер на 67 сегментах не отличил их от шума,
# решается на большом наборе.
# Впечатывать ли время в пиксели кадра — интерфейс вывода времени из NumPro.
VLM_STAMP_TIME = os.getenv("PRAXIS_VLM_STAMP_TIME", "0").lower() not in {"0", "false", "no"}
# Открытый словарь: списка классов заранее нет, модель отвечает своими словами.
# Кейс требует уметь назвать любое действие и любой предмет.
# Язык ответов модели — должен совпадать с языком эталона.
LANGUAGE = os.getenv("PRAXIS_LANGUAGE", "ru")
OPEN_VOCABULARY = os.getenv("PRAXIS_OPEN_VOCABULARY", "1").lower() not in {"0", "false", "no"}
# На сколько секунд наружу от шага брать дополнительные кадры «до» и «после».
# Нужны для глаголов состояния: «взял» и «положил» неразличимы по середине действия.
# Замер на 197 сегментах: действие 0.558 против 0.523, пара 0.426 против 0.386, предмет
# без изменений — ровно тот механизм, что закладывался. Парный тест даёт прирост +0.036
# при интервале от -0.010 до +0.081, то есть ноль внутри: направление верное, значимость
# не доказана. Включено потому, что приём бесплатный по времени, а не потому что доказан.
CONTEXT_FRAMES = float(os.getenv("PRAXIS_CONTEXT_FRAMES", "0.6"))
VLM_FRAME_LABELS = os.getenv("PRAXIS_VLM_FRAME_LABELS", "1").lower() not in {"0", "false", "no"}
# Две ступени: сначала предмет, потом действие. Выключены. Замер на 60 роликах Charades,
# 139 шагов, счёт по всем допустимым меткам (действие / предмет / пара):
#   одна ступень                      0.532 / 0.540 / 0.367
#   две ступени, действия сужены      0.511 / 0.626 / 0.317
#   две ступени, только подсказка     0.525 / 0.525 / 0.295
#   две ступени, предмет зафиксирован 0.525 / 0.619 / 0.288
# Узкий вопрос «что за предмет» действительно отвечается точнее — предмет вырастает на
# девять пунктов. Но пара награждает связность: когда обе половины спрашиваются вместе,
# модель выбирает сочетание, осмысленное целиком, а по отдельности глагол перестаёт
# согласовываться с предметом, и пара падает при обеих выросших половинах.
VLM_TWO_STAGE = os.getenv("PRAXIS_VLM_TWO_STAGE", "0").lower() not in {"0", "false", "no"}
# Пять кадров, и это измерено: больше кадров активно вредит. На 197 сегментах с
# контекстом 0.6 с точность действия 0.558 при пяти кадрах и 0.142 при восьми — падение
# вчетверо. Причина, судя по всему, в бюджете зрительных токенов модели: десять картинок
# на запрос вытесняют друг друга и промпт.
VLM_FRAMES = int(os.getenv("PRAXIS_VLM_FRAMES", "5"))
VLM_FRAME_WIDTH = int(os.getenv("PRAXIS_VLM_FRAME_WIDTH", "640"))
# Отдавать кадры шага видеотрактом модели вместо пачки независимых картинок. Смысл не в
# упаковке: видеопроцессор Qwen3-VL склеивает соседние кадры во временные патчи по два и
# сам врезает в промпт отметки времени. То есть модель видит движение между кадрами, а не
# набор стоп-кадров, — а именно движения не хватало на глаголах состояния, где «взял» и
# «положил» различимы только направлением времени.
VLM_VIDEO_MODE = os.getenv("PRAXIS_VLM_VIDEO", "1").lower() not in {"0", "false", "no"}
# Частота сплошной нарезки внутри длинного шага. Ноль — сплошной нарезки нет вовсе.
VLM_VIDEO_FPS = float(os.getenv("PRAXIS_VLM_VIDEO_FPS", "2"))
# Потолок и пол по кадрам на шаг: длинный шаг при высокой частоте не влезет в память
# карты, а короткий при низкой получил бы три снимка вместо нынешних пяти.
VLM_VIDEO_MAX_FRAMES = int(os.getenv("PRAXIS_VLM_VIDEO_MAX_FRAMES", "32"))
VLM_VIDEO_MIN_FRAMES = int(os.getenv("PRAXIS_VLM_VIDEO_MIN_FRAMES", "8"))
# С какой длины шага включается сплошная нарезка. Пошаговое сравнение на 142 шагах
# валидации, счёт с поправкой на синонимы:
#   0-1 с   51 шаг  0.373 -> 0.392  исправлено 5, испорчено 4
#   1-2 с   30      0.400 -> 0.333  исправлено 2, испорчено 4
#   2-5 с   22      0.227 -> 0.136  исправлено 1, испорчено 3
#   5-10 с  29      0.241 -> 0.276  исправлено 2, испорчено 1
#   >10 с   10      0.100 -> 0.700  исправлено 6, испорчено 0
# То есть на длинном шаге плотная нарезка чинит и ничего не ломает, на коротком портит
# вдвое чаще, чем чинит. Причина в устройстве видеотракта: временной патч склеивает пару
# соседних кадров, и движение в нём есть, только если кадры близки по времени. На шаге в
# секунду пять выборок и так идут через 0.2 с — добавлять нечего, лишние кадры лишь
# раздувают зрительную часть промпта и вытесняют инструкцию.
VLM_DENSE_MIN_SEC = float(os.getenv("PRAXIS_VLM_DENSE_MIN_SEC", "5"))
# Склейка повторов: из плотной нарезки выбрасываются кадры, где по сравнению с последним
# оставленным ничего не изменилось. Меряется теми же признаками видеоэнкодера, по которым
# ставятся границы, — они уже посчитаны на стадии нарезки и лежат в кэше, отдельная модель
# не нужна. Пустая строка отключает склейку.
VLM_SELECT = os.getenv("PRAXIS_VLM_SELECT", "1").lower() not in {"0", "false", "no"}
# Порог застоя как доля собственного распределения ролика: «кадр похож на предыдущий
# сильнее, чем 85 % соседних пар здесь». Абсолютная константа тут не годится — окна
# видеоэнкодера перекрываются на три четверти, и уровень похожести зависит от того,
# насколько подвижен ролик целиком.
VLM_SELECT_QUANTILE = float(os.getenv("PRAXIS_VLM_SELECT_QUANTILE", "0.85"))
# Потолок кадров после склейки. Жёсткий потолок вреден: при восьми кадрах выигрыш на
# крупной гранулярности пропадал (0.118 против 0.529), потому что возвращалась
# разреженность, ради устранения которой всё и делается.
VLM_SELECT_MAX = int(os.getenv("PRAXIS_VLM_SELECT_MAX", "24"))
# Детектор рук: по нему закрашивается всё, что дальше заданного радиуса от кистей. Замер
# показал, что предмет — самое слабое место именования: он верен в 21 ответе из 142, а в 60
# назван родовым словом («part», «toy car») вместо детали. Модель отвечает по общему
# впечатлению от сцены, а лежащая на столе собранная игрушка заметнее мелкой детали в руке.
# Пустая строка — кадры уходят нетронутыми.
HANDS_BASE_URL = os.getenv("PRAXIS_HANDS_BASE_URL", "")
# Радиус видимой области долями размера кисти и нижняя граница радиуса долей кадра.
HANDS_MARGIN = float(os.getenv("PRAXIS_HANDS_MARGIN", "1.5"))
HANDS_MIN_RADIUS = float(os.getenv("PRAXIS_HANDS_MIN_RADIUS", "0.12"))
# Размытие границы: резкий круг сам становится объектом, который модель пытается назвать.
HANDS_FEATHER = float(os.getenv("PRAXIS_HANDS_FEATHER", "0.02"))
# Как отмечать рабочую зону: "white" — закрасить всё вне её, "circle" — обвести овалом и
# ничего не убирать, "dim" — притушить фон. Обводка сохраняет контекст, а прошлый замер
# кропа по предмету (0.209 против 0.358) говорит, что контекст для опознания предмета нужен.
HANDS_MODE = os.getenv("PRAXIS_HANDS_MODE", "white")
# Ставка за час GPU: из неё и секунд работы моделей считается стоимость прогона,
# которую кейс требует показывать на финале.
# Предел на обработку одного ролика. Кейс требует укладываться в две минуты и требует
# у задания таймаут: зависший прогон обязан падать, а не висеть в processing вечно.
JOB_TIMEOUT_SEC = float(os.getenv("PRAXIS_JOB_TIMEOUT_SEC", "300"))
GPU_HOUR_COST = float(os.getenv("PRAXIS_GPU_HOUR_COST", "60"))
VLM_TIMEOUT = float(os.getenv("PRAXIS_VLM_TIMEOUT", "120"))
# Проба /health перед нарезкой кадров. Без неё недоступный сервис обнаруживался бы
# только после того, как ffmpeg вырезал по семь кадров на каждый шаг — секунды впустую
# на каждом ролике, пока туннель к GPU-машине не поднят.
VLM_HEALTH_TIMEOUT = float(os.getenv("PRAXIS_VLM_HEALTH_TIMEOUT", "3"))

# Писать ли в экспорт отметку о проверке шага человеком. В требованиях кейса такого
# поля нет, но заказчику важно знать, что подтверждено живым человеком, а что нет.
# Гасится одной переменной, если приёмка окажется строгой к лишним полям.
EXPORT_VERIFIED = os.getenv("PRAXIS_EXPORT_VERIFIED", "1").lower() not in {"0", "false", "no"}

# Плёнка кадров под таймлайном и разрешение сигнала движения.
FILMSTRIP_COUNT = 40
MOTION_FPS = 10

# Чем описывать кадр для сегментатора:
#   video — окна кадров через видеоэнкодер: единственные признаки, которые видят движение,
#           а не только содержимое кадра (нужен PRAXIS_VIDEO_BASE_URL),
#   embed — покадровые эмбеддинги визуального энкодера (нужен PRAXIS_CLIP_BASE_URL),
#   gray  — усреднённые серые блоки, запасной вариант без GPU.
FEATURES = os.getenv("PRAXIS_FEATURES", "video")
VIDEO_BASE_URL = os.getenv("PRAXIS_VIDEO_BASE_URL", "")
# Какую модель поднимать в сервисе признаков. Сравнение на двадцати роликах (одна сетка
# параметров, F1 без учёта меток при IoU 0.1/0.25/0.5):
#   timesformer  0.808 / 0.808 / 0.744, границы 1.48 с  ← выбрана
#   swin3d       0.820 / 0.820 / 0.725, границы 1.67 с
#   vjepa2       0.818 / 0.818 / 0.707, границы 1.88 с
#   videomae     0.814 / 0.814 / 0.594, границы 1.74 с
#   серые пиксели 0.805 / 0.785 / 0.715
# Закономерность: признаки, обученные распознавать действия на Kinetics, обходят
# самообучаемые. Ровно поэтому работы по temporal action segmentation стоят на I3D.
VIDEO_MODEL = os.getenv("PRAXIS_VIDEO_MODEL", "timesformer")
# Пороги для методов по матрице самоподобия (praxis/pipeline/similarity.py).
# Кэш признаков на диске: подбор параметров гоняет один ролик десятки раз.
FEATURE_CACHE = os.getenv("PRAXIS_FEATURE_CACHE", "1").lower() not in {"0", "false", "no"}
# Подобрано измерением, и оптимум резко зависит от гранулярности набора:
#   грубая разметка (Charades, 2.2 шага по 6 с): штраф 20 -> F1@0.5 0.402
#   атомарная (Assembly101 fine, 9.9 шага по 1 с): штраф 2 -> F1@0.5 0.401
# Рекурсивный разбор проигрывает обоим на любом пороге, лучшее 0.251.
# На сколько сигм выше среднего должен быть выброс, чтобы стать границей.
# Поправка границ по измеренной знаковой ошибке. На валидации начала оказались
# позже эталона на 0.67 с, концы почти без смещения.
# Доля от максимума движения внутри сегмента, ниже которой край считается паузой.
TRIM_SHARE = float(os.getenv("PRAXIS_TRIM_SHARE", "0.0"))
START_SHIFT = float(os.getenv("PRAXIS_START_SHIFT", "0.0"))
END_SHIFT = float(os.getenv("PRAXIS_END_SHIFT", "0.0"))
# Обучаемый детектор границ: адрес сервиса и порог на вероятность.
TAS_BASE_URL = os.getenv("PRAXIS_TAS_BASE_URL", "")
# Ансамбль: допуск совпадения разрезов, порог подтверждения и порог одиночного пика.
ENSEMBLE_TOLERANCE_SEC = float(os.getenv("PRAXIS_ENSEMBLE_TOLERANCE_SEC", "0.5"))
ENSEMBLE_CONFIRM = float(os.getenv("PRAXIS_ENSEMBLE_CONFIRM", "0.3"))
ENSEMBLE_STRONG = float(os.getenv("PRAXIS_ENSEMBLE_STRONG", "0.9"))
# Порог пика для отгружаемого ансамбля-консенсуса (минимум по двум fine-моделям, 8 Гц):
# результат почти не зависит от порога в 0.3–0.7 на 85 атомарных роликах; 0.6 вместе с
# режимом пауз по активности подобран сразу по трём наборам — снятые ролики с паузами,
# Assembly101 fine, EPIC. См. experiments/results/2026-09-05-boundary-final.md.
TAS_THRESHOLD = float(os.getenv("PRAXIS_TAS_THRESHOLD", "0.6"))
# Минимальный интервал между пиками детектора. Отдельно от минимальной длины шага:
# разрезы ставятся не реже 0.5 с, а сегменты короче 0.75 с отбрасываются.
TAS_PEAK_GAP_SEC = float(os.getenv("PRAXIS_TAS_PEAK_GAP_SEC", "0.5"))
# Полоса движения как 769-й канал детектора. Одинакова для руки человека и
# манипулятора — граница это событие захвата, а не внешний вид.
TAS_MOTION = os.getenv("PRAXIS_TAS_MOTION", "0").lower() not in {"0", "false", "no"}
# Разности признаков во времени как каналы (DDM-Net) и усреднение по временным
# масштабам на инференсе (C2F-TCN). Оба включаются флагами, чтобы сравнивать честно.
TAS_DIFF = os.getenv("PRAXIS_TAS_DIFF", "0").lower() not in {"0", "false", "no"}
TAS_TTA = os.getenv("PRAXIS_TAS_TTA", "0").lower() not in {"0", "false", "no"}
# Минимальная выраженность пика над окрестностью; 0 — обычный локальный максимум.
TAS_PROMINENCE = float(os.getenv("PRAXIS_TAS_PROMINENCE", "0"))
PEAK_SIGMA = float(os.getenv("PRAXIS_PEAK_SIGMA", "1.0"))
TSM_THRESHOLD = float(os.getenv("PRAXIS_TSM_THRESHOLD", "0.35"))
TSM_PENALTY = float(os.getenv("PRAXIS_TSM_PENALTY", "2.0"))
VIDEO_WINDOW = int(os.getenv("PRAXIS_VIDEO_WINDOW", "16"))
# Шаг 2 (сетка 8 Гц вместо 4) измерен на 60 роликах Charades: F1@0.5 0.466 против 0.452,
# границы 1.46 с против 1.63 с, но F1@0.25 0.756 против 0.783 — всё внутри доверительного
# интервала, а счёт признаков дороже на 63% (854 с против 522 с). Оставлен шаг 4.
VIDEO_STRIDE = int(os.getenv("PRAXIS_VIDEO_STRIDE", "4"))
# Кадры 32 fps при окне 16 кадров — окно 0.5 с и шаг 0.125 с (8 Гц). Против окна 1 с при
# 4 Гц: +0.05 F1@0.5 на 85 атомарных роликах (0.478 против 0.427) — короткие шаги видны
# только с таким разрешением. Ключ кэша признаков включает эти параметры.
VIDEO_FPS = float(os.getenv("PRAXIS_VIDEO_FPS", "32"))
# Сколько главных компонент оставлять от признаков. Ноль — не сжимать.
# Сжатие главными компонентами нужно самообучаемым признакам (V-JEPA, VideoMAE), где
# соседние окна похожи на 0.997. Признакам, обученным на Kinetics, оно только вредит.
COMPONENTS = int(os.getenv("PRAXIS_COMPONENTS", "0"))

# Ручки сегментатора. Штраф за отрезок — главная: он задаёт гранулярность и защищает от
# пересегментации. Значения подобраны scripts/tune.py на признаках V-JEPA 2
# (F1@0.1 и F1@0.25 = 0.818, F1@0.5 = 0.707 без учёта меток на двадцати роликах).
# Минимальная длина шага намеренно оставлена скромной: подгонять её под крупные шаги
# одного набора рискованно, гранулярность скрытого набора неизвестна.
SEGMENT_PENALTY = float(os.getenv("PRAXIS_SEGMENT_PENALTY", "0.2"))
BOUNDARY_WEIGHT = float(os.getenv("PRAXIS_BOUNDARY_WEIGHT", "0.1"))
MAX_SEGMENTS = int(os.getenv("PRAXIS_MAX_SEGMENTS", "8"))
# Ручка гранулярности вместе с TSM_PENALTY. Замерено на двух режимах:
#   бытовые сцены, 2.2 шага по 6 с   -> MIN_SEGMENT_SEC=1.5, TSM_PENALTY=8
#   атомарные действия, 9 шагов по 1 с -> MIN_SEGMENT_SEC=0.5, TSM_PENALTY=2
# По умолчанию стоит атомарный режим: кейсодатель показал ролики, где робот поднимает
# предмет, поворачивает, переносит и опускает — это секунда-две на шаг.
# Интервал между пиками детектора и фильтр коротких сегментов. С признаками 8 Гц пики
# достаточно острые для 0.5 с: 0.478 против 0.428 при 1 с на 85 атомарных роликах; на
# старых признаках 4 Гц было наоборот — см. experiments/results/2026-09-05-boundary-final.md.
MIN_SEGMENT_SEC = float(os.getenv("PRAXIS_MIN_SEGMENT_SEC", "0.75"))
# Ниже какой доли среднего движения по ролику отрезок считается паузой, а не шагом.
# Ноль отключает пропуски и возвращает сплошное покрытие таймлайна.
# Доля от среднего движения, ниже которой сегмент считается паузой и в шаги не идёт.
# Ноль отключает фильтр: на атомарной гранулярности шаги идут плотно и пауз почти нет.
IDLE_RATIO = float(os.getenv("PRAXIS_IDLE_RATIO", "0.0"))
# Паузы у обученного детектора: "activity" — шаги идут только там, где полоса движения
# выше уровня (от шумового пола ролика, с гистерезисом), разрезы детектора делят
# активные области изнутри; "none" — сплошное покрытие таймлайна. Настроено по трём
# снятым роликам с размеченными паузами: 0.62 против 0.18 у сплошной нарезки, одинаково
# на 720p и 256p. В Assembly101 и EPIC паузы размечены иначе (переходы с движением), там
# этот режим теряет (0.47 → 0.32), поэтому для таких наборов PRAXIS_IDLE_MODE=none.
# См. experiments/results/2026-09-05-boundary-final.md.
IDLE_MODE = os.getenv("PRAXIS_IDLE_MODE", "activity")
ACTIVITY_LEVEL = float(os.getenv("PRAXIS_ACTIVITY_LEVEL", "0.15"))  # старт активности: доля хода «пол → среднее» полосы движения
ACTIVITY_LOW = float(os.getenv("PRAXIS_ACTIVITY_LOW", "0.7"))  # конец активности: доля от уровня старта (гистерезис)
ACTIVITY_SMOOTH_SEC = float(os.getenv("PRAXIS_ACTIVITY_SMOOTH_SEC", "0.5"))  # сглаживание полосы движения
ACTIVITY_CLOSE_SEC = float(os.getenv("PRAXIS_ACTIVITY_CLOSE_SEC", "0.25"))  # провалы короче этого закрываются
# Обучаемый детектор пауз: тот же MS-TCN, обученный на кадры вне размеченных шагов
# (переходы с движением, ожидание). Второй сервис serve_tas с чекпоинтом
# checkpoints/gapness.pt; пусто — только неподвижность по полосе движения.
GAP_BASE_URL = os.getenv("PRAXIS_GAP_BASE_URL", "")
GAP_THRESHOLD = float(os.getenv("PRAXIS_GAP_THRESHOLD", "0.5"))
GAP_MIN_SEC = float(os.getenv("PRAXIS_GAP_MIN_SEC", "0.5"))  # короче этого пауза не считается
MERGE_GAIN = float(os.getenv("PRAXIS_MERGE_GAIN", "0"))
