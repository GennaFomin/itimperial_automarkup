# Режим мока: UI работает на фикстурах, без бэкенда.
# Нужен для сценариев контракта §8, которые живой пайплайн по требованию не
# воспроизводит: пустой результат, деградировавший прогон, упавшая задача,
# обработка дольше десяти минут.
VITE_API_MOCK=1
